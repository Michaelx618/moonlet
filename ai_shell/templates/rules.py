BASE_RULES = (
    "RULES: The filename does NOT determine what to generate. "
    "Even if the file is named test_*.py, produce what the user requested, not tests."
)

SMALL_FILE_RULE = (
    "For small data structures (linked list, stack, etc.) you may add "
    "a brief usage example ONLY if appropriate and NOT if the user "
    "asked for a specific method."
)

NO_TESTS_RULE = "Do NOT add tests or test code unless explicitly requested."

NO_PLACEHOLDERS_RULE = "Do NOT use TODOs, placeholders, or stub implementations. Provide complete working code."
